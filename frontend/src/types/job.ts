export type Job = {
      id: number;
      title: string;
      company: string;
      description: string;
      applyingDate: string;
      interviewDate: string;
      link: string;
      cvUrl?: string;
      letterUrl?: string;
      createdAt: string;
      estimatedScore: string;
      status: string;
    };